import type { CardProps } from '@adi/react-components';
import { Card } from '@adi/react-components';
import { Meta, StoryObj } from '@storybook/react';

export default {
    title: 'Components/Card',
    component: Card,
} as Meta<CardProps>;

export const Default: StoryObj<CardProps> = {};
