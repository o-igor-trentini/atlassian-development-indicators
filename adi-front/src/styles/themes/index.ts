export interface Theme {
    primaryColor: string;
    secondaryColor: string;

    borderRadius: number;
}

export const LightTheme: Theme = {
    primaryColor: 'red',
    secondaryColor: 'blue',

    borderRadius: 8,
};
