import type { SpaceProps } from '@it-adi/react-components';
import { Space } from '@it-adi/react-components';
import { Meta, StoryObj } from '@storybook/react';

export default {
    title: 'Components/Space',
    component: Space,
} as Meta<SpaceProps>;

export const Default: StoryObj<SpaceProps> = {};
