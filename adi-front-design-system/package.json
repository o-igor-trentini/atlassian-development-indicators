{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "lint": "turbo run lint --filter=!ts-config --parallel",
    "dev": "turbo run dev --filter=!ts-config --parallel",
    "build": "turbo run build --filter=!ts-config"
  },
  "devDependencies": {
    "turbo": "^1.9.1"
  }
}
