import type { ColProps } from '@it-adi/react-components';
import { Col } from '@it-adi/react-components';
import { Meta, StoryObj } from '@storybook/react';

export default {
    title: 'Components/Grid/Col',
    component: Col,
} as Meta<ColProps>;

export const Default: StoryObj<ColProps> = {};
